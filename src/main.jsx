/* Vite entry — pulls together styles, the custom <image-slot> element,
   the WebGL wallpaper background, and the React app (which self-mounts). */
import "./colors_and_type.css";
import "./styles.css";
import "./image-slot.js";
import "./wallpaper-bg.js";
import "./app.jsx";
